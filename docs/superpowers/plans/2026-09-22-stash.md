# Stash Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Een macOS 27-menubalk-app die per app status items verbergt achter één chevron, via de assessment-mode allowlist.

**Architecture:** Eén Objective-C shim kapselt het private `MenuBarClientCore`-framework in; daarboven een pure Swift-laag die uitrekent welke bundle ids zichtbaar mogen zijn (`AppInventory` − `HiddenSet`, plus altijd het eigen id), en een AppKit-laag met een chevron-status-item en een SwiftUI-instellingenvenster. De app houdt permanent één assertion vast; in- en uitklappen is niets anders dan die assertion vervangen door een met een andere allowlist.

**Tech Stack:** Swift 6.4, SwiftPM (geen Xcode), AppKit, SwiftUI via `NSHostingView`, Objective-C shim-target, `ServiceManagement`.

**Spec:** `docs/superpowers/specs/2026-09-22-stash-design.md` — lees die eerst; dit plan argumenteert eruit.

## Global Constraints

- **Doel-OS:** macOS 27.0 (build 26A428), arm64. `platforms: [.macOS("26.0")]` in `Package.swift` (SwiftPM kent geen `.v27`-enum; de stringvorm werkt).
- **Geen Xcode op deze machine** — alleen Command Line Tools met Swift 6.4. Daarom: **XCTest en swift-testing zijn niet beschikbaar.** Beide falen op het linken (`XCTest` module niet vindbaar, `_TestingInterop` niet vindbaar). Tests draaien als gewoon uitvoerbaar target: `swift run StashTests`, exitcode 0 = groen, 1 = rood. Nooit `swift test` gebruiken.
- **Bundle id:** `com.brentc22.Stash`, exact deze spelling, overal.
- **`allowedSystemItems` is altijd `0...255`.** Met `0...63` verdwijnt Screen Mirroring ongewild.
- **`MBAssessmentModeConfiguration` accepteert alleen `NSArray`.** Een `NSSet` gooit een exception.
- **Het eigen bundle id staat altijd in de allowlist**, in beide toestanden. Anders verbergt de app z'n eigen chevron en is er geen weg terug.
- Code, typen en comments in het Engels. UI-labels in het Nederlands.
- Commit na elke taak. Geen attributie-footer in commit messages (globaal uitgezet).
- `ld: warning: search path ... not found` bij elke build is normaal op een CLT-only toolchain en geen fout.

## Bestandsstructuur

| Bestand | Verantwoordelijk voor |
|---|---|
| `Package.swift` | targets en platform |
| `Sources/MenuBarShim/include/STMenuBarShim.h` | publieke ObjC-interface naar de private API |
| `Sources/MenuBarShim/STMenuBarShim.m` | `dlopen`, runtime-lookup, assertion aanmaken/invalideren |
| `Sources/StashCore/SystemItems.swift` | de `0...255`-constante, op één plek |
| `Sources/StashCore/HiddenSet.swift` | welke bundle ids verborgen horen te zijn + persistentie |
| `Sources/StashCore/Allowlist.swift` | pure functie: inventory + verborgen set + toestand → allowlist |
| `Sources/StashCore/AppInventory.swift` | draaiende apps volgen via `NSWorkspace` |
| `Sources/StashCore/Preferences.swift` | inklap-vertraging en "start bij inloggen" |
| `Sources/StashCore/MenuBarRestriction.swift` | assertion-levenscyclus bovenop de shim |
| `Sources/StashCore/MenuBarProbe.swift` | leest `trailingItems.count` uit de OS-log; alleen voor tests |
| `Sources/Stash/main.swift` | `NSApplication` opstarten als accessory |
| `Sources/Stash/AppDelegate.swift` | alles aan elkaar knopen, toestand bewaken |
| `Sources/Stash/StatusItemController.swift` | de chevron en z'n menu |
| `Sources/Stash/CollapseTimer.swift` | automatisch inklappen |
| `Sources/Stash/SettingsWindowController.swift` | het venster om de SwiftUI-view heen |
| `Sources/Stash/SettingsView.swift` | de SwiftUI-instellingen |
| `Sources/StashTests/Harness.swift` | mini-testharness |
| `Sources/StashTests/main.swift` | alle tests, aangeroepen op volgorde |
| `Makefile` | `.app` bundelen, signen, installeren |
| `Resources/Info.plist` | `LSUIElement`, bundle id, versie |

---

### Task 1: Package-skelet, testharness en shim-beschikbaarheid

**Files:**
- Create: `Package.swift`
- Create: `Sources/MenuBarShim/include/STMenuBarShim.h`
- Create: `Sources/MenuBarShim/STMenuBarShim.m`
- Create: `Sources/StashCore/StashCore.swift`
- Create: `Sources/StashTests/Harness.swift`
- Create: `Sources/StashTests/main.swift`

**Interfaces:**
- Consumes: niets
- Produces: `STMenuBarShim.isAvailable() -> Bool` (ObjC class method, in Swift `STMenuBarShim.isAvailable()`); testharness `T.test(_:_:)`, `T.expect(_:_:file:line:)`, `T.equal(_:_:_:file:line:)`, `T.finish() -> Never`

- [ ] **Step 1: Schrijf `Package.swift`**

```swift
// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "Stash",
    platforms: [.macOS("26.0")],
    targets: [
        .target(name: "MenuBarShim"),
        .target(name: "StashCore", dependencies: ["MenuBarShim"]),
        .executableTarget(name: "Stash", dependencies: ["StashCore"]),
        .executableTarget(name: "StashTests", dependencies: ["StashCore"]),
    ]
)
```

- [ ] **Step 2: Schrijf de testharness**

`Sources/StashTests/Harness.swift`:

```swift
import Foundation

/// Minimale testharness. XCTest en swift-testing zijn niet beschikbaar zonder Xcode,
/// dus tests draaien als gewoon programma: `swift run StashTests`.
enum T {
    nonisolated(unsafe) private static var failures: [String] = []
    nonisolated(unsafe) private static var passed = 0
    nonisolated(unsafe) private static var current = ""

    static func test(_ name: String, _ body: () throws -> Void) {
        current = name
        let before = failures.count
        do {
            try body()
        } catch {
            failures.append("\(name): wierp \(error)")
            print("  FAIL \(name): wierp \(error)")
            return
        }
        if failures.count == before {
            passed += 1
            print("  ok   \(name)")
        }
    }

    static func expect(_ condition: Bool, _ message: String,
                       file: StaticString = #filePath, line: UInt = #line) {
        guard !condition else { return }
        let entry = "\(current): \(message)  (\(file):\(line))"
        failures.append(entry)
        print("  FAIL \(entry)")
    }

    static func equal<V: Equatable>(_ actual: V, _ expected: V, _ message: String = "",
                                    file: StaticString = #filePath, line: UInt = #line) {
        expect(actual == expected, "\(message) verwacht \(expected), kreeg \(actual)",
               file: file, line: line)
    }

    static func finish() -> Never {
        print("\n\(passed) geslaagd, \(failures.count) gefaald")
        for f in failures { print("  - \(f)") }
        exit(failures.isEmpty ? 0 : 1)
    }
}
```

Let op het verschil met een naïeve harness: het aantal failures wordt vóór en ná `body` vergeleken. Zonder die vergelijking telt een test die via `expect` faalt toch als geslaagd, omdat `expect` niet gooit.

- [ ] **Step 3: Schrijf de falende test**

`Sources/StashTests/main.swift`:

```swift
import MenuBarShim

print("Stash tests")

T.test("shim ziet MenuBarClientCore") {
    T.expect(STMenuBarShim.isAvailable(),
             "MenuBarClientCore moet laadbaar zijn op macOS 27")
}

T.finish()
```

Voeg `"MenuBarShim"` toe aan de dependencies van `StashTests` in `Package.swift`:

```swift
.executableTarget(name: "StashTests", dependencies: ["StashCore", "MenuBarShim"]),
```

- [ ] **Step 4: Draai de test en zie hem falen**

Run: `swift run StashTests`
Expected: build-fout — `no such module 'MenuBarShim'` of `cannot find 'STMenuBarShim'`, omdat de shim nog niet bestaat.

- [ ] **Step 5: Schrijf de shim-header**

`Sources/MenuBarShim/include/STMenuBarShim.h`:

```objc
#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

/// Dunne laag om het private MenuBarClientCore-framework.
/// Alles wat met dlopen en NSClassFromString te maken heeft zit hier en nergens anders.
@interface STMenuBarShim : NSObject

/// NO wanneer het framework of een van de twee klassen ontbreekt.
/// Roep dit één keer bij het starten aan en sla het resultaat op.
+ (BOOL)isAvailable;

@end

NS_ASSUME_NONNULL_END
```

- [ ] **Step 6: Schrijf de shim-implementatie**

`Sources/MenuBarShim/STMenuBarShim.m`:

```objc
#import "include/STMenuBarShim.h"
#import <dlfcn.h>

static NSString *const kFrameworkPath =
    @"/System/Library/PrivateFrameworks/MenuBarClientCore.framework/MenuBarClientCore";

@implementation STMenuBarShim

+ (BOOL)loadFramework {
    static BOOL loaded = NO;
    static dispatch_once_t once;
    dispatch_once(&once, ^{
        loaded = dlopen(kFrameworkPath.UTF8String, RTLD_NOW) != NULL;
    });
    return loaded;
}

+ (BOOL)isAvailable {
    if (![self loadFramework]) { return NO; }
    return NSClassFromString(@"MBAssessmentModeConfiguration") != nil
        && NSClassFromString(@"MBAssessmentModeAssertion") != nil;
}

@end
```

- [ ] **Step 7: Draai de test en zie hem slagen**

Run: `swift run StashTests`
Expected:
```
Stash tests
  ok   shim ziet MenuBarClientCore

1 geslaagd, 0 gefaald
```
Exitcode 0. Controleer met `echo $?`.

- [ ] **Step 8: Voeg een lege `StashCore` toe zodat het target bouwt**

`Sources/StashCore/StashCore.swift`:

```swift
/// Versiestempel, zodat het target niet leeg is en het bouwen deterministisch blijft.
public enum StashCore {
    public static let version = "0.1.0"
}
```

Run: `swift build`
Expected: `Build complete!`

- [ ] **Step 9: Commit**

```bash
git add Package.swift Sources/
git commit -m "feat: package-skelet, testharness en shim-beschikbaarheidscheck"
```

---

### Task 2: Assertion activeren, vervangen en invalideren

Dit is de kern. Alles daarna is bediening eromheen.

**Files:**
- Modify: `Sources/MenuBarShim/include/STMenuBarShim.h`
- Modify: `Sources/MenuBarShim/STMenuBarShim.m`
- Create: `Sources/StashCore/SystemItems.swift`
- Create: `Sources/StashCore/MenuBarRestriction.swift`
- Create: `Sources/StashCore/MenuBarProbe.swift`
- Modify: `Sources/StashTests/main.swift`

**Interfaces:**
- Consumes: `STMenuBarShim.isAvailable()`
- Produces:
  - ObjC: `+[STMenuBarShim activateWithAllowedBundleIdentifiers:allowedSystemItems:completion:] -> id` (nil bij falen), `+[STMenuBarShim invalidate:]`
  - Swift: `protocol MenuBarRestricting { var isAvailable: Bool { get }; func apply(allowing: Set<String>, completion: ((Error?) -> Void)?); func clear() }`
  - Swift: `final class MenuBarRestriction: MenuBarRestricting`
  - Swift: `enum SystemItems { static let all: [NSNumber] }`
  - Swift: `enum MenuBarProbe { static func lastTrailingItemsCount(withinSeconds: Int) -> Int? }`
  - Swift: `enum MenuBarRestrictionError: Error { case unavailable, activationFailed(String) }`

- [ ] **Step 1: Breid de shim-header uit**

```objc
#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

@interface STMenuBarShim : NSObject

+ (BOOL)isAvailable;

/// Activeert een nieuwe assertion. Geeft een token terug dat je bewaart en later
/// aan +invalidate: geeft; nil wanneer het framework ontbreekt.
/// completion krijgt nil bij succes, anders de NSError van het systeem.
/// LET OP: beide arrays moeten NSArray zijn. Een NSSet gooit een exception.
+ (nullable id)activateWithAllowedBundleIdentifiers:(NSArray<NSString *> *)bundleIdentifiers
                                  allowedSystemItems:(NSArray<NSNumber *> *)systemItems
                                          completion:(void (^)(NSError *_Nullable))completion;

/// Veilig met nil.
+ (void)invalidate:(nullable id)token;

@end

NS_ASSUME_NONNULL_END
```

- [ ] **Step 2: Breid de shim-implementatie uit**

Voeg toe aan `STMenuBarShim.m`:

```objc
+ (nullable id)activateWithAllowedBundleIdentifiers:(NSArray<NSString *> *)bundleIdentifiers
                                  allowedSystemItems:(NSArray<NSNumber *> *)systemItems
                                          completion:(void (^)(NSError *_Nullable))completion {
    if (![self isAvailable]) { return nil; }

    Class configClass = NSClassFromString(@"MBAssessmentModeConfiguration");
    Class assertionClass = NSClassFromString(@"MBAssessmentModeAssertion");

    SEL initSel = NSSelectorFromString(@"initWithAllowedSystemItems:allowedBundleIdentifiers:");
    id config = [configClass alloc];
    if (![config respondsToSelector:initSel]) { return nil; }

    NSMethodSignature *initSig = [config methodSignatureForSelector:initSel];
    NSInvocation *initInv = [NSInvocation invocationWithMethodSignature:initSig];
    initInv.target = config;
    initInv.selector = initSel;
    NSArray *items = systemItems;
    NSArray *ids = bundleIdentifiers;
    [initInv setArgument:&items atIndex:2];
    [initInv setArgument:&ids atIndex:3];
    [initInv invoke];
    __unsafe_unretained id built = nil;
    [initInv getReturnValue:&built];
    id builtConfig = built;
    if (builtConfig == nil) { return nil; }

    id assertion = [[assertionClass alloc] init];
    SEL activateSel = NSSelectorFromString(@"activateWithConfiguration:completionHandler:");
    if (![assertion respondsToSelector:activateSel]) { return nil; }

    NSMethodSignature *sig = [assertion methodSignatureForSelector:activateSel];
    NSInvocation *inv = [NSInvocation invocationWithMethodSignature:sig];
    inv.target = assertion;
    inv.selector = activateSel;
    [inv setArgument:&builtConfig atIndex:2];
    id handler = [completion copy];
    [inv setArgument:&handler atIndex:3];
    [inv invoke];

    return assertion;
}

+ (void)invalidate:(nullable id)token {
    if (token == nil) { return; }
    SEL sel = NSSelectorFromString(@"invalidate");
    if ([token respondsToSelector:sel]) {
        ((void (*)(id, SEL))objc_msgSend)(token, sel);
    }
}
```

Voeg bovenaan het bestand toe: `#import <objc/message.h>`.

- [ ] **Step 3: Schrijf `SystemItems`**

`Sources/StashCore/SystemItems.swift`:

```swift
import Foundation

/// Systeemitems worden als numerieke id's doorgegeven. Bekende id's: klok 2, geluid 5,
/// wifi 6, Control Center 8. Het bereik loopt aantoonbaar boven 63 door: met alleen
/// 0...63 verdween Screen Mirroring uit de balk. Daarom een ruime marge.
public enum SystemItems {
    public static let all: [NSNumber] = (0...255).map { NSNumber(value: $0) }
}
```

- [ ] **Step 4: Schrijf het log-orakel**

`Sources/StashCore/MenuBarProbe.swift`:

```swift
import Foundation

/// MenuBarAgent logt zelf hoeveel items er in de balk staan. Dat is het enige
/// betrouwbare orakel dat we hebben zonder screenshots te vergelijken:
///
///   [com.apple.menubar:analytics] MenuBar.trailingItems.count payload=["count": 12]
///
/// Alleen bedoeld voor tests, niet voor de app zelf.
public enum MenuBarProbe {

    /// De laatst gelogde telling, of nil als er in dat venster niets gelogd is.
    /// `--info --debug` is verplicht: zonder die vlaggen laat `log show` deze regels weg.
    public static func lastTrailingItemsCount(withinSeconds seconds: Int = 30) -> Int? {
        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/usr/bin/log")
        process.arguments = [
            "show", "--last", "\(seconds)s", "--info", "--debug", "--style", "compact",
            "--predicate", "subsystem == \"com.apple.menubar\" AND category == \"analytics\"",
        ]
        let pipe = Pipe()
        process.standardOutput = pipe
        process.standardError = FileHandle.nullDevice

        do { try process.run() } catch { return nil }
        let data = pipe.fileHandleForReading.readDataToEndOfFile()
        process.waitUntilExit()

        guard let output = String(data: data, encoding: .utf8) else { return nil }
        let pattern = #"trailingItems\.count payload=\["count": (\d+)\]"#
        guard let regex = try? NSRegularExpression(pattern: pattern) else { return nil }
        let matches = regex.matches(in: output, range: NSRange(output.startIndex..., in: output))
        guard let last = matches.last, let range = Range(last.range(at: 1), in: output) else {
            return nil
        }
        return Int(output[range])
    }
}
```

Elke wijziging levert meerdere identieke regels op — één per scherm of space. Neem altijd de laatste.

- [ ] **Step 5: Schrijf de falende tests**

Voeg toe aan `Sources/StashTests/main.swift`, vóór `T.finish()`:

```swift
import Foundation
import StashCore

/// Past een allowlist toe, wacht tot de balk hertekend is, en geeft de telling terug
/// die MenuBarAgent logde. 2 seconden is ruim: in de probe stond de nieuwe telling
/// binnen ~200 ms in de log.
func applyAndCount(_ restriction: MenuBarRestriction, _ allowed: Set<String>) -> Int? {
    restriction.apply(allowing: allowed)
    Thread.sleep(forTimeInterval: 2)
    return MenuBarProbe.lastTrailingItemsCount()
}

let allRunning = Set(NSWorkspace.shared.runningApplications.compactMap(\.bundleIdentifier))

T.test("lege allowlist levert minder items op dan een volle") {
    let restriction = MenuBarRestriction()
    guard restriction.isAvailable else {
        T.expect(false, "restriction moet beschikbaar zijn op macOS 27")
        return
    }

    let wide = applyAndCount(restriction, allRunning)
    let narrow = applyAndCount(restriction, [])

    restriction.clear()
    Thread.sleep(forTimeInterval: 2)

    guard let wide, let narrow else {
        T.expect(false, "log gaf geen telling terug — draait MenuBarAgent?")
        return
    }
    T.expect(narrow < wide, "lege allowlist moet minder items opleveren: \(narrow) < \(wide)")
}

T.test("vervangen laat geen oude assertion achter") {
    let restriction = MenuBarRestriction()

    let collapsed = applyAndCount(restriction, [])
    let expanded = applyAndCount(restriction, allRunning)

    restriction.clear()
    Thread.sleep(forTimeInterval: 2)

    guard let collapsed, let expanded else {
        T.expect(false, "log gaf geen telling terug")
        return
    }
    T.expect(expanded > collapsed,
             "na vervangen moet de balk weer vol zijn: \(expanded) > \(collapsed)")
}

T.test("clear zet de balk volledig terug") {
    let restriction = MenuBarRestriction()

    let baseline = applyAndCount(restriction, allRunning)
    _ = applyAndCount(restriction, [])

    restriction.clear()
    Thread.sleep(forTimeInterval: 2)
    let restored = MenuBarProbe.lastTrailingItemsCount()

    guard let baseline, let restored else {
        T.expect(false, "log gaf geen telling terug")
        return
    }
    T.equal(restored, baseline, "na clear:")
}
```

- [ ] **Step 6: Draai de tests en zie ze falen**

Run: `swift run StashTests`
Expected: build-fout, `cannot find 'MenuBarRestriction' in scope`.

- [ ] **Step 7: Schrijf `MenuBarRestriction`**

`Sources/StashCore/MenuBarRestriction.swift`:

```swift
import Foundation
import MenuBarShim

public enum MenuBarRestrictionError: Error {
    case unavailable
    case activationFailed(String)
}

public protocol MenuBarRestricting: AnyObject {
    var isAvailable: Bool { get }
    /// Vervangt de huidige restrictie. Idempotent. Het wisselen van token gebeurt
    /// synchroon; `completion` rapporteert alleen of het systeem achteraf klaagde.
    func apply(allowing bundleIDs: Set<String>, completion: ((Error?) -> Void)?)
    /// Heft elke restrictie op; de balk wordt weer volledig getekend.
    func clear()
}

public extension MenuBarRestricting {
    func apply(allowing bundleIDs: Set<String>) { apply(allowing: bundleIDs, completion: nil) }
}

public final class MenuBarRestriction: MenuBarRestricting, @unchecked Sendable {

    private var token: AnyObject?
    private let lock = NSLock()

    public init() {}

    public var isAvailable: Bool { STMenuBarShim.isAvailable() }

    public func apply(allowing bundleIDs: Set<String>, completion: ((Error?) -> Void)? = nil) {
        guard isAvailable else {
            completion?(MenuBarRestrictionError.unavailable)
            return
        }

        let created = STMenuBarShim.activate(
            withAllowedBundleIdentifiers: bundleIDs.sorted(),
            allowedSystemItems: SystemItems.all
        ) { error in
            if let error {
                completion?(MenuBarRestrictionError.activationFailed(error.localizedDescription))
            } else {
                completion?(nil)
            }
        }

        guard let created = created as AnyObject? else {
            completion?(MenuBarRestrictionError.unavailable)
            return
        }

        // Nieuwe eerst activeren, dan pas de oude weggooien: de balk mag geen moment
        // zonder actieve restrictie zitten, anders springen alle iconen even terug.
        lock.lock()
        let old = token
        token = created
        lock.unlock()
        STMenuBarShim.invalidate(old)
    }

    public func clear() {
        lock.lock()
        let old = token
        token = nil
        lock.unlock()
        STMenuBarShim.invalidate(old)
    }

    deinit { clear() }
}
```

Bewust géén `async`. De activering is synchroon — de token bestaat zodra de shim
terugkeert — en alleen de foutmelding komt later. Een `withCheckedThrowingContinuation`
eromheen zou een continuation kunnen introduceren die twee keer of nooit hervat wordt,
en dat is een crash of een deadlock voor een waarde die we alleen willen loggen.

- [ ] **Step 8: Draai de tests**

Run: `swift run StashTests`
Expected: beide tests groen. Je ziet je menubalk tijdens de run een paar keer leeglopen en weer vollopen — dat hoort.

- [ ] **Step 9: Als `vervangen laat geen oude assertion achter` faalt**

Dan mogen twee assertions niet naast elkaar bestaan en blijft de oude de balk beperken. Draai de volgorde in `apply` om:

```swift
        lock.lock()
        let old = token
        token = nil
        lock.unlock()
        STMenuBarShim.invalidate(old)

        // en pas hierna activeren
```

Dat betekent één frame flikkering bij elke toggle. Noteer de uitkomst — welke volgorde het geworden is en waarom — in `docs/superpowers/specs/2026-09-22-stash-design.md` §4.2, waar dit als open punt staat. Draai de test opnieuw tot hij groen is.

- [ ] **Step 10: Controleer dat de balk hersteld is**

```bash
screencapture -x -D 1 /tmp/bar.png && sips -c 60 1400 --cropOffset 0 2040 /tmp/bar.png --out /tmp/bar_crop.png
```
Open `/tmp/bar_crop.png` en controleer dat alle iconen er weer staan. Blijft er iets weg: `pkill -f StashTests`.

- [ ] **Step 11: Controleer op neveneffecten buiten de balk**

Assessment mode is examenmodus. In de probe van 22-09 is niets anders waargenomen dan het
niet-tekenen van status items, maar dat was tien seconden. Draai een langere controle:

Zet tijdelijk deze test bovenaan `Sources/StashTests/main.swift` en draai `swift run StashTests`:

```swift
T.test("soak: tien minuten met een lege allowlist") {
    let restriction = MenuBarRestriction()
    restriction.apply(allowing: [])
    print("  lege allowlist actief — controleer nu handmatig, 600 seconden")
    Thread.sleep(forTimeInterval: 600)
    restriction.clear()
    Thread.sleep(forTimeInterval: 2)
    T.expect(MenuBarProbe.lastTrailingItemsCount() != nil, "log moet nog tellingen geven")
}
```

Controleer tijdens die tien minuten:

- Control Center opent en werkt
- Spotlight (⌘spatie) opent
- Focus wisselen via Control Center werkt
- Notificaties komen binnen
- Schermafbeelding maken werkt

Faalt een van deze: noteer het in de spec bij §9 en stop — dan is deze aanpak niet geschikt
en moet de spacer-fallback alsnog overwogen worden. Haal de tijdelijke test daarna weg.

- [ ] **Step 12: Commit**

```bash
git add Sources/
git commit -m "feat: assertion activeren, vervangen en invalideren met log-orakel als test"
```

---

### Task 3: HiddenSet en allowlist-berekening

Pure logica, geen OS. Dit is de plek waar de ergste valkuil uit de spec wordt afgevangen.

**Files:**
- Create: `Sources/StashCore/HiddenSet.swift`
- Create: `Sources/StashCore/Allowlist.swift`
- Modify: `Sources/StashTests/main.swift`

**Interfaces:**
- Consumes: niets
- Produces:
  - `public enum BarState { case collapsed, expanded }`
  - `public final class HiddenSet` met `init(defaults: UserDefaults)`, `var bundleIDs: Set<String> { get }`, `func hide(_ id: String)`, `func show(_ id: String)`, `func isHidden(_ id: String) -> Bool`, `func setHidden(_ hidden: Bool, for id: String)`
  - `public enum Allowlist { static func compute(running: Set<String>, hidden: Set<String>, state: BarState, ownBundleID: String) -> Set<String> }`

- [ ] **Step 1: Schrijf de falende tests**

Voeg toe aan `Sources/StashTests/main.swift`, vóór `T.finish()`:

```swift
let ownID = "com.brentc22.Stash"

T.test("uitgeklapt staat alles toe") {
    let result = Allowlist.compute(
        running: ["com.a", "com.b", "com.c"],
        hidden: ["com.b"],
        state: .expanded,
        ownBundleID: ownID
    )
    T.equal(result, ["com.a", "com.b", "com.c", ownID])
}

T.test("ingeklapt laat de verborgen apps weg") {
    let result = Allowlist.compute(
        running: ["com.a", "com.b", "com.c"],
        hidden: ["com.b"],
        state: .collapsed,
        ownBundleID: ownID
    )
    T.equal(result, ["com.a", "com.c", ownID])
}

T.test("eigen bundle id zit er altijd in, ook als hij verborgen is gemarkeerd") {
    let result = Allowlist.compute(
        running: ["com.a"],
        hidden: [ownID],
        state: .collapsed,
        ownBundleID: ownID
    )
    T.expect(result.contains(ownID), "de app mag z'n eigen chevron nooit verbergen")
}

T.test("een app die start terwijl je ingeklapt bent verdwijnt niet") {
    // Dit is de valkuil: de allowlist is een momentopname. Start er een app bij
    // en herberekenen we niet, dan staat die niet in de lijst en verdwijnt hij
    // ongevraagd. Herberekenen met de nieuwe inventory hoort hem terug te geven.
    let before = Allowlist.compute(
        running: ["com.a"], hidden: ["com.b"], state: .collapsed, ownBundleID: ownID
    )
    T.expect(!before.contains("com.nieuw"), "opzet: com.nieuw draait nog niet")

    let after = Allowlist.compute(
        running: ["com.a", "com.nieuw"], hidden: ["com.b"], state: .collapsed, ownBundleID: ownID
    )
    T.expect(after.contains("com.nieuw"),
             "een nieuw gestarte app die niet verborgen is hoort zichtbaar te blijven")
}

T.test("verborgen set overleeft opnieuw laden") {
    let suite = "com.brentc22.Stash.tests.\(UUID().uuidString)"
    guard let defaults = UserDefaults(suiteName: suite) else {
        T.expect(false, "kon geen testsuite maken"); return
    }
    defer { UserDefaults.standard.removePersistentDomain(forName: suite) }

    let first = HiddenSet(defaults: defaults)
    first.hide("com.a")
    first.hide("com.b")
    first.show("com.a")

    let second = HiddenSet(defaults: defaults)
    T.equal(second.bundleIDs, ["com.b"])
    T.expect(second.isHidden("com.b"), "com.b hoort verborgen te zijn")
    T.expect(!second.isHidden("com.a"), "com.a is weer getoond")
}
```

- [ ] **Step 2: Draai de tests en zie ze falen**

Run: `swift run StashTests`
Expected: build-fout, `cannot find 'Allowlist' in scope`.

- [ ] **Step 3: Schrijf `HiddenSet`**

`Sources/StashCore/HiddenSet.swift`:

```swift
import Foundation

/// Welke bundle ids verborgen horen te zijn wanneer de balk ingeklapt is.
/// Weet niets van het besturingssysteem; volledig testbaar.
public final class HiddenSet {

    private static let key = "hiddenBundleIDs"

    private let defaults: UserDefaults
    private var storage: Set<String>

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        self.storage = Set(defaults.stringArray(forKey: Self.key) ?? [])
    }

    public var bundleIDs: Set<String> { storage }

    public func isHidden(_ bundleID: String) -> Bool { storage.contains(bundleID) }

    public func hide(_ bundleID: String) {
        guard storage.insert(bundleID).inserted else { return }
        persist()
    }

    public func show(_ bundleID: String) {
        guard storage.remove(bundleID) != nil else { return }
        persist()
    }

    public func setHidden(_ hidden: Bool, for bundleID: String) {
        hidden ? hide(bundleID) : show(bundleID)
    }

    private func persist() {
        defaults.set(storage.sorted(), forKey: Self.key)
    }
}
```

- [ ] **Step 4: Schrijf `Allowlist`**

`Sources/StashCore/Allowlist.swift`:

```swift
import Foundation

public enum BarState {
    case collapsed
    case expanded
}

/// De enige plek waar bepaald wordt wie zichtbaar mag zijn.
public enum Allowlist {

    /// - Parameters:
    ///   - running: alles wat nu draait. Moet vers zijn: een verouderde lijst laat een
    ///     nieuw gestarte app buiten de allowlist vallen, en die verdwijnt dan ongevraagd.
    ///   - hidden: wat de gebruiker heeft aangevinkt om te verbergen.
    ///   - state: ingeklapt of uitgeklapt.
    ///   - ownBundleID: het eigen id. Staat er altijd bij, ongeacht de rest — zonder
    ///     chevron kan de gebruiker niet meer uitklappen.
    public static func compute(running: Set<String>,
                               hidden: Set<String>,
                               state: BarState,
                               ownBundleID: String) -> Set<String> {
        let base: Set<String>
        switch state {
        case .expanded:  base = running
        case .collapsed: base = running.subtracting(hidden)
        }
        return base.union([ownBundleID])
    }
}
```

- [ ] **Step 5: Draai de tests**

Run: `swift run StashTests`
Expected: alle tests groen, exitcode 0.

- [ ] **Step 6: Commit**

```bash
git add Sources/
git commit -m "feat: HiddenSet met persistentie en allowlist-berekening"
```

---

### Task 4: AppInventory

**Files:**
- Create: `Sources/StashCore/AppInventory.swift`
- Modify: `Sources/StashTests/main.swift`

**Interfaces:**
- Consumes: niets
- Produces:
  - `public struct KnownApp: Identifiable, Hashable` met `let id: String` (bundle id), `let name: String`, `var isRunning: Bool`
  - `public final class AppInventory` met `init(defaults: UserDefaults)`, `var runningBundleIDs: Set<String> { get }`, `var knownApps: [KnownApp] { get }`, `var onChange: (() -> Void)?`, `func start()`, `func stop()`, `func icon(for bundleID: String) -> NSImage?`

- [ ] **Step 1: Schrijf de falende tests**

Voeg toe aan `Sources/StashTests/main.swift`, vóór `T.finish()`:

```swift
T.test("inventory ziet draaiende apps") {
    let suite = "com.brentc22.Stash.tests.\(UUID().uuidString)"
    guard let defaults = UserDefaults(suiteName: suite) else {
        T.expect(false, "kon geen testsuite maken"); return
    }
    defer { UserDefaults.standard.removePersistentDomain(forName: suite) }

    let inventory = AppInventory(defaults: defaults)
    T.expect(inventory.runningBundleIDs.count > 10,
             "er draaien er meer dan 10, kreeg \(inventory.runningBundleIDs.count)")
    T.expect(inventory.runningBundleIDs.contains("com.apple.controlcenter"),
             "Control Center draait altijd")
}

T.test("inventory onthoudt apps die gestopt zijn") {
    let suite = "com.brentc22.Stash.tests.\(UUID().uuidString)"
    guard let defaults = UserDefaults(suiteName: suite) else {
        T.expect(false, "kon geen testsuite maken"); return
    }
    defer { UserDefaults.standard.removePersistentDomain(forName: suite) }

    defaults.set(["com.verdwenen.app": "Verdwenen App"], forKey: "knownAppNames")
    let inventory = AppInventory(defaults: defaults)

    let known = inventory.knownApps.first { $0.id == "com.verdwenen.app" }
    T.expect(known != nil, "een eerder gezien app hoort in de lijst te blijven staan")
    T.equal(known?.isRunning, false)
    T.equal(known?.name, "Verdwenen App")
}

T.test("inventory meldt een wijziging") {
    let suite = "com.brentc22.Stash.tests.\(UUID().uuidString)"
    guard let defaults = UserDefaults(suiteName: suite) else {
        T.expect(false, "kon geen testsuite maken"); return
    }
    defer { UserDefaults.standard.removePersistentDomain(forName: suite) }

    let inventory = AppInventory(defaults: defaults)
    var called = 0
    inventory.onChange = { called += 1 }
    inventory.refresh()
    T.equal(called, 1, "refresh hoort onChange aan te roepen:")
}
```

- [ ] **Step 2: Draai de tests en zie ze falen**

Run: `swift run StashTests`
Expected: build-fout, `cannot find 'AppInventory' in scope`.

- [ ] **Step 3: Schrijf `AppInventory`**

`Sources/StashCore/AppInventory.swift`:

```swift
import AppKit

public struct KnownApp: Identifiable, Hashable {
    public let id: String       // bundle identifier
    public let name: String
    public var isRunning: Bool
}

/// Houdt bij welke apps draaien en welke we ooit gezien hebben.
///
/// Bestaat om één reden: de allowlist is een momentopname van bundle ids. Start er
/// een app bij nadat we die lijst hebben doorgegeven, dan staat die er niet in en
/// verdwijnt hij ongevraagd uit de balk. Elke launch moet dus een herberekening
/// uitlokken.
public final class AppInventory {

    private static let namesKey = "knownAppNames"

    private let defaults: UserDefaults
    private var names: [String: String]
    private var observers: [NSObjectProtocol] = []

    public var onChange: (() -> Void)?

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        self.names = defaults.dictionary(forKey: Self.namesKey) as? [String: String] ?? [:]
        rememberRunningNames()
    }

    deinit { stop() }

    public func start() {
        let center = NSWorkspace.shared.notificationCenter
        for name in [NSWorkspace.didLaunchApplicationNotification,
                     NSWorkspace.didTerminateApplicationNotification] {
            let token = center.addObserver(forName: name, object: nil, queue: .main) { [weak self] _ in
                self?.refresh()
            }
            observers.append(token)
        }
    }

    public func stop() {
        let center = NSWorkspace.shared.notificationCenter
        observers.forEach { center.removeObserver($0) }
        observers.removeAll()
    }

    public func refresh() {
        rememberRunningNames()
        onChange?()
    }

    public var runningBundleIDs: Set<String> {
        Set(eligibleApplications().compactMap(\.bundleIdentifier))
    }

    public var knownApps: [KnownApp] {
        let running = runningBundleIDs
        let ids = Set(names.keys).union(running)
        return ids
            .map { KnownApp(id: $0, name: names[$0] ?? $0, isRunning: running.contains($0)) }
            .sorted { $0.name.localizedStandardCompare($1.name) == .orderedAscending }
    }

    public func icon(for bundleID: String) -> NSImage? {
        NSWorkspace.shared.runningApplications
            .first { $0.bundleIdentifier == bundleID }?
            .icon
    }

    /// Apps die een status item kúnnen hebben. Welke er werkelijk één heeft is zonder
    /// Accessibility niet te weten, dus we tonen de ruimere verzameling.
    private func eligibleApplications() -> [NSRunningApplication] {
        NSWorkspace.shared.runningApplications.filter {
            $0.bundleIdentifier != nil
                && ($0.activationPolicy == .regular || $0.activationPolicy == .accessory)
        }
    }

    private func rememberRunningNames() {
        var changed = false
        for app in eligibleApplications() {
            guard let id = app.bundleIdentifier else { continue }
            let name = app.localizedName ?? id
            if names[id] != name {
                names[id] = name
                changed = true
            }
        }
        if changed { defaults.set(names, forKey: Self.namesKey) }
    }
}
```

- [ ] **Step 4: Draai de tests**

Run: `swift run StashTests`
Expected: alle tests groen.

- [ ] **Step 5: Commit**

```bash
git add Sources/
git commit -m "feat: AppInventory volgt draaiende apps en onthoudt gestopte"
```

---

### Task 5: De app zelf — chevron en toggle

Na deze taak is het een werkende app.

**Files:**
- Create: `Sources/Stash/main.swift`
- Create: `Sources/Stash/AppDelegate.swift`
- Create: `Sources/Stash/StatusItemController.swift`

**Interfaces:**
- Consumes: `MenuBarRestriction`, `HiddenSet`, `AppInventory`, `Allowlist.compute(running:hidden:state:ownBundleID:)`, `BarState`
- Produces:
  - `final class StatusItemController` met `init(onToggle:onSettings:onQuit:)`, `func render(state: BarState, available: Bool)`
  - `final class AppDelegate: NSObject, NSApplicationDelegate` met `func toggle()`, `func rebuild()`

- [ ] **Step 1: Schrijf `StatusItemController`**

`Sources/Stash/StatusItemController.swift`:

```swift
import AppKit
import StashCore

/// De chevron in de balk. Weet niets van de private API.
final class StatusItemController {

    private let item: NSStatusItem
    private let onToggle: () -> Void
    private let onSettings: () -> Void
    private let onQuit: () -> Void

    init(onToggle: @escaping () -> Void,
         onSettings: @escaping () -> Void,
         onQuit: @escaping () -> Void) {
        self.onToggle = onToggle
        self.onSettings = onSettings
        self.onQuit = onQuit
        self.item = NSStatusBar.system.statusItem(withLength: NSStatusItem.squareLength)

        item.button?.target = self
        item.button?.action = #selector(buttonPressed)
        item.button?.sendAction(on: [.leftMouseUp, .rightMouseUp])
    }

    func render(state: BarState, available: Bool) {
        guard let button = item.button else { return }
        let symbol: String
        let description: String
        if !available {
            symbol = "exclamationmark.triangle"
            description = "Verbergen niet beschikbaar"
        } else {
            switch state {
            case .collapsed: symbol = "chevron.left";  description = "Toon verborgen items"
            case .expanded:  symbol = "chevron.right"; description = "Verberg items"
            }
        }
        button.image = NSImage(systemSymbolName: symbol, accessibilityDescription: description)
        button.image?.isTemplate = true
        button.toolTip = description
    }

    @objc private func buttonPressed() {
        guard let event = NSApp.currentEvent else { onToggle(); return }
        if event.type == .rightMouseUp {
            showMenu()
        } else {
            onToggle()
        }
    }

    private func showMenu() {
        guard let button = item.button else { return }
        let menu = NSMenu()
        menu.addItem(withTitle: "Instellingen…", action: #selector(settingsPressed), keyEquivalent: ",")
            .target = self
        menu.addItem(.separator())
        menu.addItem(withTitle: "Stash stoppen", action: #selector(quitPressed), keyEquivalent: "q")
            .target = self
        // popUp in plaats van item.menu toewijzen: dat laatste laat het menu ook bij
        // een linkerklik verschijnen, en dan kun je niet meer togglen.
        menu.popUp(positioning: nil,
                   at: NSPoint(x: 0, y: button.bounds.height + 4),
                   in: button)
    }

    @objc private func settingsPressed() { onSettings() }
    @objc private func quitPressed() { onQuit() }
}
```

- [ ] **Step 2: Schrijf `AppDelegate`**

`Sources/Stash/AppDelegate.swift`:

```swift
import AppKit
import StashCore

let ownBundleID = "com.brentc22.Stash"

final class AppDelegate: NSObject, NSApplicationDelegate {

    private let restriction = MenuBarRestriction()
    private let hidden = HiddenSet()
    private let inventory = AppInventory()
    private var statusItem: StatusItemController!
    private var state: BarState = .collapsed

    func applicationDidFinishLaunching(_ notification: Notification) {
        statusItem = StatusItemController(
            onToggle: { [weak self] in self?.toggle() },
            onSettings: { [weak self] in self?.showSettings() },
            onQuit: { [weak self] in self?.quit() }
        )
        statusItem.render(state: state, available: restriction.isAvailable)

        inventory.onChange = { [weak self] in self?.rebuild() }
        inventory.start()
        rebuild()
    }

    func applicationWillTerminate(_ notification: Notification) {
        restriction.clear()
    }

    func toggle() {
        state = (state == .collapsed) ? .expanded : .collapsed
        statusItem.render(state: state, available: restriction.isAvailable)
        rebuild()
    }

    /// Herberekent de allowlist en past hem toe. Wordt aangeroepen bij elke toggle,
    /// bij elke app-launch en na elke wijziging in de verborgen set.
    func rebuild() {
        guard restriction.isAvailable else { return }
        let allowed = Allowlist.compute(
            running: inventory.runningBundleIDs,
            hidden: hidden.bundleIDs,
            state: state,
            ownBundleID: ownBundleID
        )
        restriction.apply(allowing: allowed) { [weak self] error in
            guard let error else { return }
            NSLog("Stash: kon restrictie niet toepassen: \(error)")
            DispatchQueue.main.async {
                guard let self else { return }
                self.statusItem.render(state: self.state, available: false)
            }
        }
    }

    private func showSettings() {
        // Gevuld in taak 7.
    }

    private func quit() {
        restriction.clear()
        NSApp.terminate(nil)
    }
}
```

- [ ] **Step 3: Schrijf `main.swift`**

`Sources/Stash/main.swift`:

```swift
import AppKit

let app = NSApplication.shared
// Accessory: geen Dock-icoon, geen menubalk-menu's, wel een status item.
app.setActivationPolicy(.accessory)
let delegate = AppDelegate()
app.delegate = delegate
app.run()
```

- [ ] **Step 4: Bouw en draai**

Run: `swift build && ./.build/debug/Stash`
Expected: er verschijnt een chevron in je menubalk. Er is nog niets verborgen, want `HiddenSet` is leeg.

- [ ] **Step 5: Verifieer handmatig dat verbergen werkt**

Zet tijdelijk één app in de verborgen set en herstart:

```bash
defaults write com.brentc22.Stash hiddenBundleIDs -array "com.apple.weather.menu"
swift build && ./.build/debug/Stash
```

Expected: het weer-icoon is weg. Klik de chevron: het komt terug. Klik opnieuw: het gaat weer weg.

Maak een screenshot van beide toestanden ter bevestiging:

```bash
screencapture -x -D 1 /tmp/stash_a.png && sips -c 60 1400 --cropOffset 0 2040 /tmp/stash_a.png --out /tmp/stash_a_crop.png
```

- [ ] **Step 6: Verifieer de launch-tijdens-ingeklapt-casus**

Laat Stash ingeklapt draaien en start een app die je **niet** verborgen hebt (bijvoorbeeld Rekenmachine).
Expected: die app verschijnt gewoon in de balk en verdwijnt niet. Gebeurt dat wel, dan wordt `rebuild()` niet aangeroepen op `didLaunchApplicationNotification` — controleer `inventory.start()`.

- [ ] **Step 7: Ruim de testinstelling op**

```bash
defaults delete com.brentc22.Stash hiddenBundleIDs
```

- [ ] **Step 8: Commit**

```bash
git add Sources/Stash/
git commit -m "feat: chevron-status-item dat de balk in- en uitklapt"
```

---

### Task 6: Automatisch inklappen

**Files:**
- Create: `Sources/Stash/CollapseTimer.swift`
- Modify: `Sources/Stash/AppDelegate.swift`
- Modify: `Sources/StashTests/main.swift`

**Interfaces:**
- Consumes: `BarState`
- Produces:
  - `public enum CollapseDelay: Int, CaseIterable` met cases `.never = 0`, `.after5 = 5`, `.after10 = 10`, `.after30 = 30`, en `var label: String`
  - `final class CollapseTimer` met `init(onFire:)`, `func schedule(delay: CollapseDelay)`, `func cancel()`
  - `Preferences.collapseDelay: CollapseDelay` (get/set via `UserDefaults`)

- [ ] **Step 1: Schrijf de falende test**

Voeg toe aan `Sources/StashTests/main.swift`, vóór `T.finish()`:

```swift
T.test("CollapseDelay rondreist door UserDefaults") {
    let suite = "com.brentc22.Stash.tests.\(UUID().uuidString)"
    guard let defaults = UserDefaults(suiteName: suite) else {
        T.expect(false, "kon geen testsuite maken"); return
    }
    defer { UserDefaults.standard.removePersistentDomain(forName: suite) }

    let prefs = Preferences(defaults: defaults)
    T.equal(prefs.collapseDelay, .after10, "standaard:")

    prefs.collapseDelay = .never
    T.equal(Preferences(defaults: defaults).collapseDelay, .never)

    prefs.collapseDelay = .after30
    T.equal(Preferences(defaults: defaults).collapseDelay, .after30)
}

T.test("alle vertragingen hebben een Nederlands label") {
    for delay in CollapseDelay.allCases {
        T.expect(!delay.label.isEmpty, "\(delay) mist een label")
    }
    T.equal(CollapseDelay.never.label, "Nooit")
    T.equal(CollapseDelay.after10.label, "Na 10 seconden")
}
```

- [ ] **Step 2: Draai de test en zie hem falen**

Run: `swift run StashTests`
Expected: build-fout, `cannot find 'Preferences' in scope`.

- [ ] **Step 3: Schrijf `CollapseDelay` en `Preferences` in StashCore**

`Sources/StashCore/Preferences.swift`:

```swift
import Foundation

public enum CollapseDelay: Int, CaseIterable, Sendable {
    case never = 0
    case after5 = 5
    case after10 = 10
    case after30 = 30

    public var label: String {
        switch self {
        case .never:   return "Nooit"
        case .after5:  return "Na 5 seconden"
        case .after10: return "Na 10 seconden"
        case .after30: return "Na 30 seconden"
        }
    }
}

public final class Preferences {

    private static let collapseDelayKey = "collapseDelaySeconds"

    private let defaults: UserDefaults

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
    }

    public var collapseDelay: CollapseDelay {
        get {
            guard defaults.object(forKey: Self.collapseDelayKey) != nil else { return .after10 }
            return CollapseDelay(rawValue: defaults.integer(forKey: Self.collapseDelayKey)) ?? .after10
        }
        set { defaults.set(newValue.rawValue, forKey: Self.collapseDelayKey) }
    }
}
```

- [ ] **Step 4: Draai de test**

Run: `swift run StashTests`
Expected: groen.

- [ ] **Step 5: Schrijf `CollapseTimer`**

`Sources/Stash/CollapseTimer.swift`:

```swift
import Foundation
import StashCore

/// Klapt de balk vanzelf weer in. Eén timer, altijd eerst geannuleerd voor een nieuwe.
final class CollapseTimer {

    private var timer: Timer?
    private let onFire: () -> Void

    init(onFire: @escaping () -> Void) {
        self.onFire = onFire
    }

    func schedule(delay: CollapseDelay) {
        cancel()
        guard delay != .never else { return }
        timer = Timer.scheduledTimer(withTimeInterval: TimeInterval(delay.rawValue),
                                     repeats: false) { [weak self] _ in
            self?.onFire()
        }
    }

    func cancel() {
        timer?.invalidate()
        timer = nil
    }

    deinit { cancel() }
}
```

- [ ] **Step 6: Knoop de timer aan `AppDelegate`**

Voeg toe aan de properties:

```swift
    private let preferences = Preferences()
    private var collapseTimer: CollapseTimer!
```

In `applicationDidFinishLaunching`, vóór `rebuild()`:

```swift
        collapseTimer = CollapseTimer { [weak self] in
            guard let self, self.state == .expanded else { return }
            self.toggle()
        }
```

Vervang `toggle()` door:

```swift
    func toggle() {
        state = (state == .collapsed) ? .expanded : .collapsed
        statusItem.render(state: state, available: restriction.isAvailable)
        if state == .expanded {
            collapseTimer.schedule(delay: preferences.collapseDelay)
        } else {
            collapseTimer.cancel()
        }
        rebuild()
    }
```

- [ ] **Step 7: Verifieer handmatig**

```bash
defaults write com.brentc22.Stash hiddenBundleIDs -array "com.apple.weather.menu"
defaults write com.brentc22.Stash collapseDelaySeconds -int 5
swift build && ./.build/debug/Stash
```

Expected: klik de chevron, het weer-icoon komt terug, en na 5 seconden verdwijnt het vanzelf weer en draait de chevron om.

- [ ] **Step 8: Ruim op en commit**

```bash
defaults delete com.brentc22.Stash hiddenBundleIDs
defaults delete com.brentc22.Stash collapseDelaySeconds
git add Sources/
git commit -m "feat: automatisch inklappen na een instelbare vertraging"
```

---

### Task 7: Instellingenvenster

**Files:**
- Create: `Sources/Stash/SettingsView.swift`
- Create: `Sources/Stash/SettingsWindowController.swift`
- Modify: `Sources/Stash/AppDelegate.swift`

**Interfaces:**
- Consumes: `AppInventory.knownApps`, `AppInventory.icon(for:)`, `HiddenSet.setHidden(_:for:)`, `Preferences.collapseDelay`, `CollapseDelay.allCases`
- Produces:
  - `final class SettingsModel: ObservableObject` met `@Published var apps: [KnownApp]`, `@Published var hiddenIDs: Set<String>`, `@Published var collapseDelay: CollapseDelay`, `func setHidden(_ hidden: Bool, for id: String)`, `func refresh()`
  - `struct SettingsView: View`
  - `final class SettingsWindowController` met `init(model:)`, `func show()`

- [ ] **Step 1: Schrijf het model en de view**

`Sources/Stash/SettingsView.swift`:

```swift
import SwiftUI
import StashCore

final class SettingsModel: ObservableObject {

    @Published var apps: [KnownApp] = []
    @Published var hiddenIDs: Set<String> = []
    @Published var collapseDelay: CollapseDelay {
        didSet {
            preferences.collapseDelay = collapseDelay
        }
    }

    private let inventory: AppInventory
    private let hidden: HiddenSet
    private let preferences: Preferences
    private let onChange: () -> Void

    init(inventory: AppInventory,
         hidden: HiddenSet,
         preferences: Preferences,
         onChange: @escaping () -> Void) {
        self.inventory = inventory
        self.hidden = hidden
        self.preferences = preferences
        self.onChange = onChange
        self.collapseDelay = preferences.collapseDelay
        refresh()
    }

    func refresh() {
        apps = inventory.knownApps.filter { $0.id != ownBundleID }
        hiddenIDs = hidden.bundleIDs
    }

    func setHidden(_ isHidden: Bool, for bundleID: String) {
        hidden.setHidden(isHidden, for: bundleID)
        hiddenIDs = hidden.bundleIDs
        onChange()
    }

    func icon(for bundleID: String) -> NSImage? { inventory.icon(for: bundleID) }
}

struct SettingsView: View {

    @ObservedObject var model: SettingsModel

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            header
            Divider()
            list
            Divider()
            footer
        }
        .frame(width: 420, height: 520)
    }

    private var header: some View {
        VStack(alignment: .leading, spacing: 4) {
            Text("Verbergen").font(.headline)
            Text("Vink aan wat achter de chevron verdwijnt. Een app met meerdere iconen "
                 + "gaat als geheel weg — dat is een beperking van macOS, niet van Stash.")
                .font(.caption)
                .foregroundStyle(.secondary)
                .fixedSize(horizontal: false, vertical: true)
        }
        .padding(16)
    }

    private var list: some View {
        List(model.apps) { app in
            Toggle(isOn: Binding(
                get: { model.hiddenIDs.contains(app.id) },
                set: { model.setHidden($0, for: app.id) }
            )) {
                HStack(spacing: 8) {
                    if let icon = model.icon(for: app.id) {
                        Image(nsImage: icon).resizable().frame(width: 16, height: 16)
                    } else {
                        Image(systemName: "app.dashed").frame(width: 16, height: 16)
                    }
                    Text(app.name)
                    if !app.isRunning {
                        Text("niet actief").font(.caption).foregroundStyle(.tertiary)
                    }
                }
            }
            .toggleStyle(.checkbox)
        }
        .listStyle(.inset)
    }

    private var footer: some View {
        HStack {
            Text("Automatisch inklappen")
            Spacer()
            Picker("", selection: $model.collapseDelay) {
                ForEach(CollapseDelay.allCases, id: \.rawValue) { delay in
                    Text(delay.label).tag(delay)
                }
            }
            .labelsHidden()
            .frame(width: 180)
        }
        .padding(16)
    }
}
```

- [ ] **Step 2: Schrijf de venstercontroller**

`Sources/Stash/SettingsWindowController.swift`:

```swift
import AppKit
import SwiftUI

/// Een gewoon NSWindow om de SwiftUI-view heen. De app is een accessory zonder
/// SwiftUI-App-levenscyclus, dus het venster wordt met de hand gemaakt.
final class SettingsWindowController {

    private var window: NSWindow?
    private let model: SettingsModel

    init(model: SettingsModel) {
        self.model = model
    }

    func show() {
        model.refresh()

        if let window {
            window.makeKeyAndOrderFront(nil)
            NSApp.activate(ignoringOtherApps: true)
            return
        }

        let window = NSWindow(
            contentRect: NSRect(x: 0, y: 0, width: 420, height: 520),
            styleMask: [.titled, .closable],
            backing: .buffered,
            defer: false
        )
        window.title = "Stash"
        window.contentView = NSHostingView(rootView: SettingsView(model: model))
        window.center()
        window.isReleasedWhenClosed = false
        window.makeKeyAndOrderFront(nil)
        NSApp.activate(ignoringOtherApps: true)
        self.window = window
    }
}
```

- [ ] **Step 3: Knoop het aan `AppDelegate`**

Voeg toe aan de properties:

```swift
    private var settingsWindow: SettingsWindowController!
```

In `applicationDidFinishLaunching`, na `inventory.start()`:

```swift
        let model = SettingsModel(
            inventory: inventory,
            hidden: hidden,
            preferences: preferences
        ) { [weak self] in
            self?.rebuild()
        }
        settingsWindow = SettingsWindowController(model: model)
```

Vervang `showSettings()`:

```swift
    private func showSettings() {
        settingsWindow.show()
    }
```

- [ ] **Step 4: Bouw en verifieer handmatig**

Run: `swift build && ./.build/debug/Stash`

Expected:
1. Rechtsklik op de chevron → "Instellingen…" opent een venster met een applijst.
2. Vink een app aan → die verdwijnt meteen uit de balk (mits je ingeklapt bent).
3. Vink hem uit → hij komt meteen terug.
4. Zet "Automatisch inklappen" op 5 seconden, sluit het venster, klik de chevron → na 5 seconden klapt hij vanzelf in.
5. Stop de app en start opnieuw → je aangevinkte apps staan er nog.

- [ ] **Step 5: Commit**

```bash
git add Sources/Stash/
git commit -m "feat: instellingenvenster met applijst en inklap-vertraging"
```

---

### Task 8: Bundelen, login item en installeren

**Files:**
- Create: `Makefile`
- Create: `Resources/Info.plist`
- Create: `Resources/make-icon.sh`
- Modify: `Sources/Stash/AppDelegate.swift`
- Modify: `Sources/Stash/SettingsView.swift`
- Create: `README.md`

**Interfaces:**
- Consumes: `Preferences`
- Produces: `Preferences.launchAtLogin: Bool`, `Stash.app` in `/Applications`

- [ ] **Step 1: Schrijf `Resources/Info.plist`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>Stash</string>
    <key>CFBundleIdentifier</key>
    <string>com.brentc22.Stash</string>
    <key>CFBundleName</key>
    <string>Stash</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleIconFile</key>
    <string>Stash</string>
    <key>LSMinimumSystemVersion</key>
    <string>26.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSHighResolutionCapable</key>
    <true/>
</dict>
</plist>
```

- [ ] **Step 2: Schrijf een icoongenerator**

`Resources/make-icon.sh` — maakt een `.icns` zonder externe tools:

```bash
#!/bin/zsh
set -e
DIR=$(mktemp -d)/Stash.iconset
mkdir -p "$DIR"

# Eenvoudig chevron-icoon als SVG, via qlmanage naar PNG geen optie zonder GUI;
# daarom een PDF via de tekstlaag van sips heen. Simpelste betrouwbare route:
# een vierkant met een chevron, getekend met Swift.
cat > /tmp/stash_icon.swift <<'SWIFT'
import AppKit
let sizes = [16, 32, 64, 128, 256, 512, 1024]
for size in sizes {
    let image = NSImage(size: NSSize(width: size, height: size))
    image.lockFocus()
    NSColor.black.setFill()
    let radius = CGFloat(size) * 0.22
    NSBezierPath(roundedRect: NSRect(x: 0, y: 0, width: size, height: size),
                 xRadius: radius, yRadius: radius).fill()
    let config = NSImage.SymbolConfiguration(pointSize: CGFloat(size) * 0.5, weight: .semibold)
    if let chevron = NSImage(systemSymbolName: "chevron.left", accessibilityDescription: nil)?
        .withSymbolConfiguration(config) {
        var rect = NSRect(x: (CGFloat(size) - chevron.size.width) / 2,
                          y: (CGFloat(size) - chevron.size.height) / 2,
                          width: chevron.size.width, height: chevron.size.height)
        NSColor.white.set()
        chevron.draw(in: rect)
        _ = rect
    }
    image.unlockFocus()
    guard let tiff = image.tiffRepresentation,
          let rep = NSBitmapImageRep(data: tiff),
          let png = rep.representation(using: .png, properties: [:]) else { continue }
    let path = CommandLine.arguments[1] + "/icon_\(size)x\(size).png"
    try? png.write(to: URL(fileURLWithPath: path))
}
SWIFT

swift /tmp/stash_icon.swift "$DIR"
# Retina-varianten zijn kopieën van de dubbele maat.
for pair in "16 32" "32 64" "128 256" "256 512" "512 1024"; do
    set -- $pair
    cp "$DIR/icon_$2x$2.png" "$DIR/icon_$1x$1@2x.png" 2>/dev/null || true
done
iconutil -c icns "$DIR" -o "$(dirname "$0")/Stash.icns"
echo "geschreven: $(dirname "$0")/Stash.icns"
```

Run: `chmod +x Resources/make-icon.sh && ./Resources/make-icon.sh`
Expected: `Resources/Stash.icns` bestaat. Lukt dit niet, laat dan `CFBundleIconFile` weg uit de plist en ga door — een ontbrekend icoon blokkeert niets, want `LSUIElement` betekent dat hij toch niet in de Dock staat.

- [ ] **Step 3: Schrijf de `Makefile`**

```makefile
APP      := Stash
BUNDLE   := $(APP).app
CONTENTS := $(BUNDLE)/Contents

.PHONY: all build bundle sign install run clean test

all: bundle

build:
	swift build -c release

bundle: build
	rm -rf $(BUNDLE)
	mkdir -p $(CONTENTS)/MacOS $(CONTENTS)/Resources
	cp .build/release/$(APP) $(CONTENTS)/MacOS/$(APP)
	cp Resources/Info.plist $(CONTENTS)/Info.plist
	-cp Resources/$(APP).icns $(CONTENTS)/Resources/$(APP).icns
	$(MAKE) sign

sign:
	codesign --force --deep --sign - $(BUNDLE)
	codesign --verify --verbose $(BUNDLE)

install: bundle
	rm -rf /Applications/$(BUNDLE)
	cp -R $(BUNDLE) /Applications/
	@echo "geïnstalleerd in /Applications/$(BUNDLE)"

run: bundle
	open $(BUNDLE)

test:
	swift run StashTests

clean:
	rm -rf .build $(BUNDLE)
```

- [ ] **Step 4: Voeg "start bij inloggen" toe aan `Preferences`**

In `Sources/StashCore/Preferences.swift`, voeg toe aan `Preferences`:

```swift
import ServiceManagement

extension Preferences {
    /// Leest de echte status bij het systeem, niet een eigen vlaggetje — die twee
    /// lopen anders uit elkaar zodra de gebruiker het in Systeeminstellingen wijzigt.
    public var launchAtLogin: Bool {
        get { SMAppService.mainApp.status == .enabled }
        set {
            do {
                if newValue {
                    try SMAppService.mainApp.register()
                } else {
                    try SMAppService.mainApp.unregister()
                }
            } catch {
                NSLog("Stash: login item wijzigen mislukt: \(error)")
            }
        }
    }
}
```

`SMAppService` werkt alleen vanuit een gebundelde, gesignde app. Vanuit `swift run` gooit hij een fout; dat is verwacht en wordt gelogd, niet gecrasht.

- [ ] **Step 5: Voeg de schakelaar toe aan de instellingen**

In `SettingsModel`, voeg toe:

```swift
    @Published var launchAtLogin: Bool {
        didSet { preferences.launchAtLogin = launchAtLogin }
    }
```

en in `init`, vóór `refresh()`:

```swift
        self.launchAtLogin = preferences.launchAtLogin
```

In `SettingsView.footer`, vervang de `HStack` door:

```swift
    private var footer: some View {
        VStack(spacing: 12) {
            HStack {
                Text("Automatisch inklappen")
                Spacer()
                Picker("", selection: $model.collapseDelay) {
                    ForEach(CollapseDelay.allCases, id: \.rawValue) { delay in
                        Text(delay.label).tag(delay)
                    }
                }
                .labelsHidden()
                .frame(width: 180)
            }
            Toggle("Starten bij inloggen", isOn: $model.launchAtLogin)
                .frame(maxWidth: .infinity, alignment: .leading)
        }
        .padding(16)
    }
```

- [ ] **Step 6: Bouw, installeer en verifieer**

```bash
make install
open /Applications/Stash.app
```

Expected:
1. De chevron verschijnt.
2. Rechtsklik → Instellingen opent.
3. "Starten bij inloggen" aanzetten geeft geen foutmelding in `log stream --process Stash`.
4. `swift run StashTests` is nog steeds groen.

Controleer of het login item geregistreerd is:

```bash
sfltool dumpbtm 2>/dev/null | grep -i stash || echo "niet gevonden (heeft mogelijk sudo nodig)"
```

- [ ] **Step 7: Schrijf de README**

`README.md`:

```markdown
# Stash

Verbergt menubalk-items op macOS 27 achter één chevron.

macOS 27 tekent de menubalk als één venster in plaats van één venster per icoon.
Daarmee vielen Ice, Bartender en Hidden Bar tegelijk om: die leunden allemaal op het
kunnen opsommen en verplaatsen van die losse vensters. Stash gebruikt in plaats
daarvan de allowlist achter assessment mode — de examenmodus waarmee scholen de balk
kaal zetten — en laat het systeem zelf bepalen wat er getekend wordt.

## Bouwen

Command Line Tools met Swift 6.4 volstaan; Xcode is niet nodig.

    make install
    open /Applications/Stash.app

## Tests

    make test

XCTest en swift-testing zitten alleen in Xcode, dus de tests draaien als gewoon
programma. Exitcode 0 is groen.

## Grenzen

- Per app, niet per icoon. Een app met drie status items gaat als geheel weg.
- Geen herordenen — de allowlist bepaalt of iets getekend wordt, niet waar.
- `MenuBarClientCore` is een privaat framework. Verdwijnt het, dan toont de chevron
  een waarschuwingsdriehoek en verbergt Stash niets; hij crasht niet.
```

- [ ] **Step 8: Commit**

```bash
git add Makefile Resources/ README.md Sources/
git commit -m "feat: app-bundel, icoon, login item en installatie via make"
```

- [ ] **Step 9: Push**

```bash
git push
```

---

## Verificatie na afloop

Draai deze drie dingen en lees de uitvoer voordat je iets "af" noemt:

1. `make test` → exitcode 0
2. `make install && open /Applications/Stash.app` → chevron verschijnt, toggle werkt in beide richtingen
3. Screenshot-bewijs van beide toestanden:

```bash
screencapture -x -D 1 /tmp/a.png && sips -c 60 1400 --cropOffset 0 2040 /tmp/a.png --out /tmp/a_crop.png
```

Stop Stash daarna één keer volledig (`Stash stoppen` in het rechtsklikmenu) en controleer dat álle iconen terug zijn. Zo niet: `pkill Stash` en kijk of ze dan terugkomen — dan blijft er een assertion hangen en moet `applicationWillTerminate` nagekeken worden.
